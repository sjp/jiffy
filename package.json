{
  "name": "jiffy",
  "version": "0.3.0",
  "private": true,
  "description": "Browser extension (Firefox & Chrome) that gives animated GIFs, WebP, APNG & AVIF video-like playback controls (play, pause, frame-step, seek).",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs --firefox && node scripts/build.mjs --chrome",
    "build:firefox": "node scripts/build.mjs --firefox",
    "build:chrome": "node scripts/build.mjs --chrome",
    "dev:firefox": "node scripts/build.mjs --firefox --watch",
    "dev:chrome": "node scripts/build.mjs --chrome --watch",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json --noEmit",
    "lint": "web-ext lint -s dist-firefox/",
    "lint:js": "oxlint",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "test": "node scripts/test.mjs",
    "test:smoke": "node scripts/smoke.mjs",
    "pack": "rm -f jiffy-firefox.zip jiffy-chrome.zip && (cd dist-firefox && zip -rX ../jiffy-firefox.zip .) && (cd dist-chrome && zip -rX ../jiffy-chrome.zip .)"
  },
  "dependencies": {
    "gifuct-js": "^2.1.2",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@puppeteer/browsers": "^3.2.2",
    "@types/firefox-webext-browser": "^143.0.0",
    "@types/jsdom": "^30.0.0",
    "@types/node": "^26.4.1",
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "puppeteer-core": "^24.43.1",
    "typescript": "^7.0.2",
    "web-ext": "^10.6.0"
  },
  "engines": {
    "node": ">=26"
  }
}
