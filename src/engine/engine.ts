// Playback engine: state + rAF loop + play/pause/step/seek + subscribe (issue 08).
import type { Engine, Frame } from './types';

/**
 * Create a playback engine over a decoded frame array.
 * Stub — implemented in issue 08.
 */
export function createEngine(_frames: Frame[]): Engine {
  throw new Error('createEngine: not implemented (issue 08)');
}
