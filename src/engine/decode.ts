// gifuct-js wrapper + precompute to full-canvas bitmaps (issue 03).
import type { DecodeResult } from './types';

/**
 * Decode GIF bytes into pre-composited frames + total duration.
 * Stub — implemented in issue 03.
 */
export async function decode(_bytes: ArrayBuffer): Promise<DecodeResult> {
  throw new Error('decode: not implemented (issue 03)');
}
